# Software Versions

Original analytical environment:

- Python 3.10.
- SciPy 1.15.3 was used for the final Phase 06 hypothesis tests.

For a fresh public rerun, install dependencies from `requirements.txt` and record the output of:

```powershell
python --version
python -m pip freeze
```
