# Article Data Availability Statement

The curated dataset, configuration files and analysis scripts required to reproduce the reported statistical tables are available in the public repository. Full raw simulator and control logs are not included in Git due to size and machine-specific artifacts; an inventory of expected raw files is provided in `data/raw_manifest/` and can be linked to an external archive if deposited.
